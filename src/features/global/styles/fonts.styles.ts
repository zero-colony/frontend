import { css } from 'styled-components';

const fontProperty = css`
  font-style: normal;
  font-variant: normal;
  font-stretch: normal;
  font-family: 'Play', sans-serif;
  font-weight: 700;
  font-size: 18px;
  line-height: 14px;
  letter-spacing: 0;

  margin: 0;
`;

export { fontProperty };
